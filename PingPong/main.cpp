#include <stdio.h> // We can use C libraries in C++
#include <iostream> // We don't have to use iostream.h


int main(){
    std::cout << "Hello World, this is literally so much more complicated that just printf, but ok lol" <<std::endl;
    return 0;
}
